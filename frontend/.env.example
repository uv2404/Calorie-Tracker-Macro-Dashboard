# ----------------------------------------------------------------------------
# Frontend environment variables (copy to `.env`)
# ----------------------------------------------------------------------------
# Base URL for the backend API.
# Leave as /api to use the Vite dev proxy (recommended for local dev).
# In production point this at your deployed backend, e.g. https://api.example.com/api
VITE_API_BASE_URL=/api
